# 🎯 برومبت تحويل المحاضرات - النظام المحدث (v2)

هذا المستند هو الدليل المرجعي لتحويل أي محتوى طبي أو تمريضي إلى كود HTML متوافق تماماً مع نظام المنصة الجديد.

---

## 🏗️ الهيكل العام للمحتوى (Current System)

النظام مصمم ليعرض المحتوى بأسلوب "English First" مع ترجمة عربية ذكية مدمجة. 

### 1. العناوين (Headings)
يتم استخدام العناوين لتنظيم المحاضرة هرمياً، مع أيقونات تعبيرية.

- **العنوان الرئيسي (h2):** للأقسام الكبيرة.
- **العنوان الفرعي (h3):** للتفاصيل داخل الأقسام.
- **العنوان البسيط (h4):** للنقاط الجانبية.

**القاعدة:** الأيقونة والعنوان الإنجليزي في سطر، والترجمة العربية (`.ar-title`) في سطر جديد تماماً تحتهم.

```html
<!-- مثال لعنوان h2 -->
<h2><i class="fas fa-notes-medical"></i> Clinical Manifestations <span class="ar-title">(الأعراض الإكلينيكية)</span></h2>

<!-- مثال لعنوان h3 -->
<h3><i class="fas fa-check-circle"></i> Early Signs <span class="ar-title">(العلامات المبكرة)</span></h3>
```

### 2. القوائم (Lists - ol, ul)
- **ul (Unordered):** للقوائم النقطية.
- **ol (Ordered):** للقوائم الرقمية (تظهر الأرقام بالإنجليزية LTR).

**القاعدة الصارمة:** كل عنصر `<li>` يجب أن يحتوي على `div` كلاس `en` وفي سطر منفصل `div` كلاس `ar`.

```html
<ul>
    <li>
        <div class="en">Increase fluid intake during the day.</div>
        <div class="ar">زيادة تناول السوائل خلال اليوم.</div>
    </li>
    <li>
        <div class="en">Monitor heart rate every 4 hours.</div>
        <div class="ar">متابعة ضربات القلب كل 4 ساعات.</div>
    </li>
</ul>
```

### 3. الجداول (Tables)
الجداول تظهر من اليسار لليمين (LTR) لسهولة قراءة المصطلحات الطبية.

**القاعدة:** المحتوى الإنجليزي في الأعلى، والترجمة العربية توضع في الـ `div` المخصص `ar-sub` مباشرة تحت النص الإنجليزي داخل نفس الخلية.

```html
<table>
    <thead>
        <tr>
            <th>Factor (العامل)</th>
            <th>Impact (التأثير)</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                Age
                <div class="ar-sub">السن</div>
            </td>
            <td>
                Metabolism decreases with age
                <div class="ar-sub">التمثيل الغذائي بيقل مع السن</div>
            </td>
        </tr>
    </tbody>
</table>
```

### 4. الفقرات العادية (Paragraphs)
إذا كان النص فقرة عادية خارج القوائم، استخدم نفس نظام الـ `div`.

```html
<div class="en">Introduction to Pediatric Nursing focuses on child health.</div>
<div class="ar">مقدمة تمريض الأطفال بتركزي على صحة الطفل.</div>
```

---

## 🎨 قائمة الأيقونات المقترحة (Font Awesome 6)
- الأهداف: `fa-bullseye`
- المميزات: `fa-star`
- الأعراض: `fa-thermometer-half`
- العلاج: `fa-pills`
- ملاحظات: `fa-info-circle`
- تحذير: `fa-exclamation-triangle`
- تعريفات: `fa-book-open`

---

## ⚠️ ملاحظات هامة عند الكتابة:
1. **لا تستخدم** علامات الترقيم العربية (مثل ؟) داخل الكلاس `en`.
2. **الترجمة العربية** يفضل أن تكون بلهجة مصرية بيضاء (تبسيط المصطلحات) لتسهيل الفهم.
3. **الجداول** لا تكثر من عدد الأعمدة (3-4 بحد أقصى) لضمان العرض الجيد على الموبايل.
4. **الأرقام** داخل `div.en` تظهر دائماً بالإنجليزية.
