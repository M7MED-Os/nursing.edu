-- ============================================================
-- 📝 إضافة حقول النبذة المختصرة (Bio)
-- ============================================================
-- إضافة خانة Bio للطلاب والشلل لجعل الموقع أكثر تفاعلية

-- 1. إضافة Bio للطلاب
ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS bio TEXT DEFAULT NULL;

-- 2. إضافة Bio للشلل
ALTER TABLE squads 
ADD COLUMN IF NOT EXISTS bio TEXT DEFAULT NULL;

-- ملاحظة: الحقول اختيارية (NULL) ويمكن للمستخدم تركها فارغة
